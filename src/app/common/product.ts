export class Product {
  id? : number;
  sku?: string;
  productName?: string;
  description?: string;
  unitPrice?: number;
  imageUrl?: string;
  active?: boolean;
  unitsInStock?:number;
  dateCreated?: Date;
  lastUpdate?: Date;
}
