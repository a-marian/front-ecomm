export class Product {
  productId? : number;
  sku?: string;
  productName?: string;
  description?: string;
  unitPrice?: number;
  imageUrl?: string;
  active?: boolean;
  unitsInStock?:number;
  dateCreated?: Date;
  lastUpdate?: Date;
}
